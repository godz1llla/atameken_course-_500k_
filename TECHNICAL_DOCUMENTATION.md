# 📚 Техническая документация ATAMEKEN STUDY CRM

## 📋 Содержание

1. [Обзор проекта](#обзор-проекта)
2. [Архитектура системы](#архитектура-системы)
3. [Технологический стек](#технологический-стек)
4. [Структура проекта](#структура-проекта)
5. [Система роутинга](#система-роутинга)
6. [База данных](#база-данных)
7. [Основные компоненты](#основные-компоненты)
8. [Роли пользователей](#роли-пользователей)
9. [API и маршруты](#api-и-маршруты)
10. [Безопасность](#безопасность)
11. [Мультиязычность](#мультиязычность)
12. [Примеры использования](#примеры-использования)

---

## 🎯 Обзор проекта

**ATAMEKEN STUDY CRM** — это полнофункциональная система управления обучением (LMS) и CRM-система, разработанная на чистом PHP без использования фреймворков. Система предназначена для управления онлайн-обучением, студентами, преподавателями, курсами, расписанием и финансовыми операциями.

### Основные возможности:
- ✅ Управление пользователями (администраторы, преподаватели, студенты, менеджеры)
- ✅ Создание и управление курсами с модулями и уроками
- ✅ Система тестирования (обычные тесты и недельные тесты)
- ✅ Домашние задания с системой оценивания
- ✅ Расписание занятий с календарем
- ✅ Система сообщений между пользователями
- ✅ Достижения и сертификаты
- ✅ Финансовый модуль (подписки, платежи)
- ✅ Отчеты для преподавателей (расчет зарплаты)
- ✅ Посещаемость студентов
- ✅ Мультиязычность (казахский, русский, английский)

---

## 🏗️ Архитектура системы

Проект построен на архитектурном паттерне **MVC (Model-View-Controller)**:

```
┌─────────────────────────────────────────┐
│           index.php (Entry Point)       │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│         Router (core/Router.php)        │
│  - Парсинг URL                          │
│  - Определение контроллера и метода    │
└─────────────────┬───────────────────────┘
                  │
        ┌─────────┴─────────┐
        │                   │
        ▼                   ▼
┌──────────────┐    ┌──────────────┐
│  Controller  │    │    Model     │
│  (Business   │◄───┤  (Database   │
│   Logic)     │    │   Access)    │
└──────┬───────┘    └──────────────┘
       │
       ▼
┌──────────────┐
│    View      │
│  (Template)  │
└──────────────┘
```

### Компоненты архитектуры:

1. **Router** (`core/Router.php`)
   - Парсит URL из GET-параметра `url`
   - Определяет контроллер и метод
   - Передает параметры в метод

2. **Controller** (`core/Controller.php`)
   - Обрабатывает HTTP-запросы
   - Вызывает модели для работы с данными
   - Передает данные в представления
   - Управляет редиректами и JSON-ответами

3. **Model** (`core/Model.php`)
   - Работа с базой данных через PDO
   - CRUD операции
   - Бизнес-логика работы с данными

4. **View** (`views/`)
   - HTML-шаблоны с PHP
   - Разделение на layouts и страницы
   - Динамический контент через переменные

---

## 💻 Технологический стек

### Backend:
- **PHP 7.4+** (чистый PHP, без фреймворков)
- **MySQL 5.7+** (база данных)
- **PDO** (работа с БД)

### Frontend:
- **HTML5, CSS3**
- **JavaScript (Vanilla JS, jQuery)**
- **Bootstrap 3** (UI компоненты)
- **FullCalendar** (календарь расписания)
- **Ion Icons** (иконки)
- **TinyMCE** (редактор текста)

### Дополнительные библиотеки:
- **SweetAlert** (красивые алерты)
- **Toastr** (уведомления)
- **Chosen** (стилизованные select)
- **Highcharts** (графики)

---

## 📁 Структура проекта

### Полная структура директорий и файлов:

```
LMSSS_AKUUU/
├── index.php                    # 🚪 Точка входа в приложение
│
├── config/                      # ⚙️ Конфигурационные файлы
│   ├── config.php               # Основные настройки приложения
│   └── database.php             # Подключение к базе данных (PDO)
│
├── core/                        # 🔧 Ядро системы (MVC фреймворк)
│   ├── Router.php               # Роутинг: парсинг URL, определение контроллера/метода
│   ├── Controller.php           # Базовый класс контроллера (общие методы)
│   ├── Model.php                # Базовый класс модели (PDO, CRUD операции)
│   ├── Session.php              # Управление сессиями (авторизация, данные пользователя)
│   └── Language.php             # Мультиязычность (загрузка переводов, функция __())
│
├── controllers/                 # 🎮 Контроллеры (бизнес-логика, обработка запросов)
│   ├── AdminController.php      # Админ-панель: пользователи, курсы, группы, расписание
│   ├── TeacherController.php    # Панель учителя: курсы, расписание, сообщения, отчеты
│   ├── StudentController.php    # Панель студента: курсы, тесты, домашние задания
│   ├── ManagerController.php    # Панель менеджера: студенты, группы
│   ├── AuthController.php       # Авторизация: вход, регистрация, выход
│   ├── TestController.php       # Обычные тесты: прохождение, результаты
│   └── WeeklyTestController.php # Недельные тесты: прохождение, результаты
│
├── models/                      # 📊 Модели (работа с базой данных)
│   ├── User.php                 # Пользователи: CRUD, авторизация, роли
│   ├── Course.php               # Курсы: создание, редактирование, список
│   ├── Module.php               # Модули курса: структура курса
│   ├── Lesson.php               # Уроки: контент, материалы
│   ├── Test.php                 # Тесты: вопросы, варианты ответов
│   ├── WeeklyTest.php           # Недельные тесты: специальные тесты
│   ├── Schedule.php             # Расписание: занятия, календарь, отчеты учителя
│   ├── Attendance.php           # Посещаемость: отметка присутствия
│   ├── Group.php                 # Группы: создание, управление
│   ├── GroupMember.php          # Участники групп: добавление/удаление
│   ├── Message.php              # Сообщения: чат между пользователями
│   ├── Homework.php             # Домашние задания: создание, редактирование
│   ├── HomeworkSubmission.php   # Ответы на ДЗ: загрузка, оценивание
│   ├── Achievement.php          # Достижения: награды студентов
│   ├── Certificate.php         # Сертификаты: генерация, выдача
│   ├── Payment.php              # Платежи: финансовые операции
│   ├── SubscriptionPlan.php    # Тарифные планы: подписки
│   ├── StudentSubscription.php  # Подписки студентов: активные подписки
│   ├── UserCourse.php           # Связь пользователь-курс: прогресс
│   ├── UserProgress.php         # Прогресс обучения: прохождение уроков
│   └── Statistics.php          # Статистика: аналитика по системе
│
├── views/                       # 🎨 Представления (HTML шаблоны)
│   ├── layouts/                 # Общие макеты
│   │   ├── header.php          # Общий заголовок: меню, навигация, пользователь
│   │   └── footer.php          # Общий футер: скрипты, закрытие HTML
│   │
│   ├── admin/                   # Страницы администратора (19 файлов)
│   │   ├── dashboard.php        # Главная панель админа: статистика
│   │   ├── users.php            # Список пользователей
│   │   ├── user_form.php        # Форма создания/редактирования пользователя
│   │   ├── courses.php          # Список курсов
│   │   ├── course_form.php      # Форма создания/редактирования курса
│   │   ├── manage_course.php    # Управление курсом: модули, уроки, тесты
│   │   ├── groups.php           # Список групп
│   │   ├── group_form.php       # Форма создания/редактирования группы
│   │   ├── group_details.php    # Детали группы: участники, расписание
│   │   ├── schedule.php         # Расписание занятий (календарь)
│   │   ├── enrollments.php      # Записи студентов на курсы
│   │   ├── achievements.php     # Управление достижениями
│   │   └── ... (другие страницы)
│   │
│   ├── teacher/                 # Страницы преподавателя (10 файлов)
│   │   ├── dashboard.php        # Главная панель учителя: курсы, статистика
│   │   ├── schedule.php         # Расписание учителя (календарь)
│   │   ├── messages.php         # Список сообщений/чатов
│   │   ├── conversation.php    # Страница чата с пользователем
│   │   ├── reports.php          # Отчеты: статистика, расчет зарплаты
│   │   ├── manage_course.php    # Управление курсом (просмотр)
│   │   ├── course_students.php  # Список студентов курса
│   │   ├── homework_form.php    # Форма создания/редактирования ДЗ
│   │   ├── homework_submissions.php # Ответы студентов на ДЗ
│   │   └── grade_submission.php # Оценивание ответа на ДЗ
│   │
│   ├── student/                 # Страницы студента (16 файлов)
│   │   ├── dashboard.php        # Главная панель студента: курсы, прогресс
│   │   ├── course.php           # Страница курса: модули, уроки
│   │   ├── lesson.php           # Страница урока: контент, материалы
│   │   ├── test.php             # Прохождение теста
│   │   ├── test_result.php      # Результат теста
│   │   ├── schedule.php         # Расписание студента (календарь)
│   │   ├── statistics.php       # Статистика студента: прогресс
│   │   ├── achievements.php     # Достижения студента
│   │   ├── certificates.php     # Сертификаты студента
│   │   ├── my_teachers.php      # Список учителей студента
│   │   ├── messages.php         # Список сообщений/чатов
│   │   ├── conversation.php     # Страница чата с пользователем
│   │   ├── profile.php          # Профиль студента
│   │   └── ... (другие страницы)
│   │
│   ├── manager/                 # Страницы менеджера (1 файл)
│   │   └── dashboard.php        # Главная панель менеджера
│   │
│   ├── auth/                     # Авторизация (2 файла)
│   │   ├── login.php            # Страница входа
│   │   └── register.php        # Страница регистрации
│   │
│   └── landing/                 # Лендинг (1 файл)
│       └── index.php            # Главная страница сайта (публичная)
│
├── public/                      # 🌐 Публичные файлы (доступны через веб-сервер)
│   │
│   ├── css/                     # Стили CSS
│   │   ├── style.css            # Основные стили системы (2446 строк)
│   │   └── landing.css          # Стили лендинг-страницы
│   │
│   ├── js/                      # JavaScript файлы
│   │   └── main.js              # Основной JS: меню, формы, интерактивность
│   │
│   ├── images/                  # Изображения
│   │   └── logo.png             # Логотип системы
│   │
│   ├── uploads/                 # Загруженные пользователями файлы
│   │   ├── avatars/             # Аватары пользователей
│   │   ├── course_images/       # Изображения курсов
│   │   ├── documents/           # Документы (материалы уроков)
│   │   └── achievement_icons/   # Иконки достижений
│   │
│   ├── bootstrap/               # Bootstrap 3 (CSS/JS фреймворк)
│   │   ├── min.css              # Стили Bootstrap
│   │   └── min.js               # JavaScript Bootstrap
│   │
│   ├── jquery.min.js            # jQuery библиотека
│   ├── jquery-ui.min.css        # jQuery UI стили
│   ├── jquery-ui.min.js         # jQuery UI скрипты
│   │
│   ├── fullcalendar/            # FullCalendar (календарь)
│   │   ├── min.css              # Стили календаря
│   │   ├── min.js               # JavaScript календаря
│   │   └── moment.js           # Библиотека для работы с датами
│   │
│   ├── ionicons/                # Ion Icons (иконки)
│   │   └── ionicons.min.css     # Стили иконок
│   │
│   ├── tinymce/                 # TinyMCE (редактор текста)
│   │   ├── tinymce.min.js       # Основной скрипт редактора
│   │   └── tinymce.ru.js         # Русская локализация
│   │
│   ├── sweetalert/              # SweetAlert (красивые алерты)
│   │   ├── css.css               # Стили алертов
│   │   └── min.js                # JavaScript алертов
│   │
│   ├── toastr/                  # Toastr (уведомления)
│   │   ├── toastr.css            # Стили уведомлений
│   │   └── toastr.js             # JavaScript уведомлений
│   │
│   ├── chosen/                  # Chosen (стилизованные select)
│   │   ├── chosen.css            # Стили
│   │   └── chosen.js            # JavaScript
│   │
│   ├── highcharts/               # Highcharts (графики)
│   │   └── min.js                # JavaScript графиков
│   │
│   ├── datapicker/              # Datepicker (выбор даты)
│   │   ├── datepicker3.min.css   # Стили
│   │   └── datepicker.*.js       # JavaScript (разные языки)
│   │
│   ├── datetime-picker/          # DateTime Picker
│   │   ├── bootstrap-datetimepicker.css
│   │   └── bootstrap-datetimepicker.min.js
│   │
│   ├── colorpicker/              # Color Picker (выбор цвета)
│   │   ├── spectrum.min.css
│   │   └── spectrum.min.js
│   │
│   ├── tagsinput/                # Tags Input (теги)
│   │   ├── bootstrap-tagsinput.css
│   │   └── bootstrap-tagsinput.min.js
│   │
│   ├── fa/                       # Font Awesome (иконки)
│   │   ├── fa.min.css
│   │   └── animate.css           # Анимации
│   │
│   ├── flaticon/                 # Flaticon (иконки)
│   │   └── flaticon.css
│   │
│   ├── plugins/                  # Дополнительные плагины
│   │   ├── jquery.inputmask.bundle.min.js  # Маски ввода
│   │   ├── jquery.inputmask-multi.min.js
│   │   ├── jquery.maskedinput.min.js
│   │   ├── phone-mask.init.js     # Инициализация масок телефона
│   │   └── plupload.min.js       # Загрузка файлов
│   │
│   ├── socket.io/                # Socket.IO (WebSocket)
│   │   ├── socket.io.js
│   │   └── options.js
│   │
│   └── yii/                     # Yii фреймворк компоненты (legacy)
│
├── database/                     # 🗄️ SQL скрипты для создания БД
│   ├── schema.sql                # Основная схема БД (таблицы)
│   ├── complete_schema.sql       # Полная схема со всеми таблицами
│   ├── groups_table.sql          # Таблица групп
│   ├── group_members_table.sql   # Таблица участников групп
│   ├── schedules_table.sql       # Таблица расписания
│   ├── homeworks_table.sql       # Таблица домашних заданий
│   ├── payments_table.sql        # Таблица платежей
│   ├── subscription_plans_table.sql      # Таблица тарифных планов
│   ├── student_subscriptions_table.sql    # Таблица подписок студентов
│   ├── student_subscriptions_add_is_paid.sql  # Добавление поля is_paid
│   ├── users_add_manager_role.sql         # Добавление роли менеджера
│   ├── users_add_rate_per_class.sql       # Добавление ставки учителя
│   └── weekly_tests_update.sql           # Обновление недельных тестов
│
├── lang/                         # 🌍 Файлы переводов (мультиязычность)
│   ├── kk.php                    # Казахский язык (массив переводов)
│   ├── ru.php                    # Русский язык (массив переводов)
│   └── en.php                    # Английский язык (массив переводов)
│
├── TEMPLATE_ATAMEKEN/            # 📐 Исходный HTML шаблон (референс)
│   ├── index.html                # Главная страница шаблона
│   ├── lessons.html              # Пример страницы уроков
│   ├── clients.html              # Пример страницы клиентов
│   ├── groups.html                # Пример страницы групп
│   ├── teachers.html             # Пример страницы учителей
│   └── ... (CSS/JS библиотеки из шаблона)
│
└── Документация/                 # 📚 Документация проекта
    ├── TECHNICAL_DOCUMENTATION.md # Техническая документация (этот файл)
    ├── INSTALL.md                 # Инструкция по установке
    ├── AI_DOCUMENTATION.md        # Документация для AI
    ├── DESIGN_DOCUMENTATION.md   # Документация по дизайну
    └── README.md                  # Основной README
```

### 📝 Подробное описание директорий и файлов:

#### 🚪 **index.php** (Точка входа)
- **Назначение**: Единственная точка входа в приложение
- **Функции**:
  - Подключение автозагрузки классов
  - Инициализация сессии
  - Подключение конфигурации
  - Запуск роутера (`Router::dispatch()`)
- **Принцип работы**: Все запросы проходят через этот файл

#### ⚙️ **config/** (Конфигурация)
- **config.php**: 
  - Базовые настройки приложения
  - Константы, пути, режим отладки
- **database.php**: 
  - Параметры подключения к БД (хост, имя БД, пользователь, пароль)
  - Создание PDO соединения
  - Обработка ошибок подключения

#### 🔧 **core/** (Ядро системы)
- **Router.php**: 
  - Парсинг URL из GET-параметра `url`
  - Определение контроллера и метода
  - Передача параметров в метод контроллера
- **Controller.php**: 
  - Базовый класс для всех контроллеров
  - Методы: `model()`, `view()`, `redirect()`, `json()`
  - Проверка авторизации
- **Model.php**: 
  - Базовый класс для всех моделей
  - Работа с PDO (подготовленные запросы)
  - CRUD операции: `find()`, `create()`, `update()`, `delete()`
- **Session.php**: 
  - Управление сессиями PHP
  - Методы: `set()`, `get()`, `has()`, `remove()`, `destroy()`
  - Работа с пользователем: `getUserId()`, `getUserRole()`
- **Language.php**: 
  - Загрузка файлов переводов из `lang/`
  - Функция `__()` для получения перевода
  - Определение текущего языка

#### 🎮 **controllers/** (Бизнес-логика)
Каждый контроллер отвечает за свою область:

- **AdminController.php**: 
  - Управление пользователями (CRUD)
  - Управление курсами, модулями, уроками
  - Управление группами и расписанием
  - Просмотр статистики и отчетов
- **TeacherController.php**: 
  - Просмотр своих курсов
  - Управление расписанием
  - Создание домашних заданий
  - Оценивание ответов студентов
  - Отчеты по работе (расчет зарплаты)
  - Сообщения со студентами
- **StudentController.php**: 
  - Просмотр доступных курсов
  - Прохождение уроков и тестов
  - Загрузка домашних заданий
  - Просмотр расписания
  - Достижения и сертификаты
  - Сообщения с учителями
- **ManagerController.php**: 
  - Просмотр студентов
  - Управление группами
- **AuthController.php**: 
  - Вход в систему (`login()`)
  - Регистрация (`register()`)
  - Выход (`logout()`)
  - Проверка авторизации

#### 📊 **models/** (Работа с БД)
Каждая модель соответствует таблице в БД:

- **User.php**: 
  - Таблица `users`
  - Методы: `createUser()`, `findByEmail()`, `updateUser()`, `getAllUsers()`
  - Проверка пароля, хеширование
- **Course.php**: 
  - Таблица `courses`
  - Методы: `getAll()`, `getById()`, `create()`, `update()`
  - Связь с учителем
- **Schedule.php**: 
  - Таблица `schedules`
  - Методы: `getEventsForCalendar()`, `getTeacherReportData()`
  - Расчет статистики для отчетов учителя
- **Message.php**: 
  - Таблица `messages`
  - Методы: `getConversations()`, `getMessages()`, `sendMessage()`
  - Чат между пользователями
- И так далее для каждой модели...

#### 🎨 **views/** (HTML шаблоны)
- **layouts/header.php**: 
  - Общий заголовок всех страниц
  - Навигационное меню (зависит от роли)
  - Профиль пользователя, выход
  - Подключение CSS файлов
- **layouts/footer.php**: 
  - Общий футер
  - Подключение JavaScript файлов
  - Закрытие HTML структуры
- **admin/**, **teacher/**, **student/**, **manager/**: 
  - Страницы для каждой роли
  - Используют `header.php` и `footer.php`
  - Динамический контент через переменные из контроллера

#### 🌐 **public/** (Публичные файлы)
- **css/style.css**: 
  - Все стили системы (2446 строк)
  - Стили для всех компонентов: карточки, кнопки, таблицы, формы
  - Адаптивный дизайн (медиа-запросы)
  - Стили календаря, чата, тестов
- **js/main.js**: 
  - Основная JavaScript логика
  - Открытие/закрытие меню (мобильная версия)
  - Обработка форм
  - AJAX запросы
- **uploads/**: 
  - Файлы, загруженные пользователями
  - Структура: `avatars/`, `course_images/`, `documents/`, `achievement_icons/`
  - Защита: доступ только через контроллер

#### 🗄️ **database/** (SQL скрипты)
- **schema.sql**: 
  - Основная схема БД
  - Создание всех таблиц
- **complete_schema.sql**: 
  - Полная схема со всеми таблицами и связями
- Остальные файлы: 
  - Дополнительные миграции
  - Добавление новых полей/таблиц

#### 🌍 **lang/** (Переводы)
- **kk.php**, **ru.php**, **en.php**: 
  - Массивы с переводами всех строк интерфейса
  - Структура: `'key' => 'Translation'`
  - Использование: `__('key')` в шаблонах

#### 📐 **TEMPLATE_ATAMEKEN/** (Референс шаблона)
- Исходный HTML/CSS/JS шаблон
- Использовался как основа для дизайна системы
- Содержит примеры страниц и стили

---

## 🛣️ Система роутинга

### Принцип работы:

Система использует **кастомный роутер без `.htaccess`**. Все маршруты проходят через `index.php` с GET-параметром `url`.

### Формат URL:

```
/index.php?url=controller/method/param1/param2
```

### Примеры маршрутов:

| URL | Контроллер | Метод | Параметры |
|-----|-----------|-------|-----------|
| `/index.php?url=auth/login` | `AuthController` | `login()` | - |
| `/index.php?url=admin/users` | `AdminController` | `users()` | - |
| `/index.php?url=student/course/5` | `StudentController` | `course(5)` | `5` |
| `/index.php?url=teacher/manageCourse/3` | `TeacherController` | `manageCourse(3)` | `3` |

### Алгоритм роутинга:

1. **Парсинг URL** (`Router::parseUrl()`)
   ```php
   // URL: /index.php?url=admin/users
   // Результат: ['admin', 'users']
   ```

2. **Определение контроллера**
   ```php
   // Первый элемент → AdminController
   // Файл: controllers/AdminController.php
   ```

3. **Определение метода**
   ```php
   // Второй элемент → метод users()
   // Если метода нет → используется index()
   ```

4. **Передача параметров**
   ```php
   // Остальные элементы → параметры метода
   // URL: /index.php?url=student/course/5
   // → StudentController::course(5)
   ```

### Код роутера:

```php
// core/Router.php
class Router {
    public function dispatch() {
        $url = $this->parseUrl();
        
        // Определение контроллера
        if (isset($url[0]) && file_exists('controllers/' . ucfirst($url[0]) . 'Controller.php')) {
            $this->controller = ucfirst($url[0]) . 'Controller';
            unset($url[0]);
        }
        
        // Загрузка контроллера
        require_once 'controllers/' . $this->controller . '.php';
        $this->controller = new $this->controller;
        
        // Определение метода
        if (isset($url[1])) {
            if (method_exists($this->controller, $url[1])) {
                $this->method = $url[1];
                unset($url[1]);
            }
        }
        
        // Параметры
        $this->params = $url ? array_values($url) : [];
        
        // Вызов метода
        call_user_func_array([$this->controller, $this->method], $this->params);
    }
}
```

---

## 🗄️ База данных

### Основные таблицы:

#### 1. **users** - Пользователи
```sql
- id (INT, PRIMARY KEY)
- email (VARCHAR, UNIQUE)
- password (VARCHAR) - хеш bcrypt
- first_name, last_name (VARCHAR)
- role (ENUM: 'admin', 'teacher', 'student', 'manager')
- avatar (VARCHAR) - путь к аватару
- rate_per_class (DECIMAL) - ставка для учителей
- created_at (TIMESTAMP)
```

#### 2. **courses** - Курсы
```sql
- id (INT, PRIMARY KEY)
- title (VARCHAR)
- description (TEXT)
- teacher_id (INT, FOREIGN KEY → users.id)
- image (VARCHAR)
- is_published (TINYINT)
- created_at (TIMESTAMP)
```

#### 3. **modules** - Модули курса
```sql
- id (INT, PRIMARY KEY)
- course_id (INT, FOREIGN KEY → courses.id)
- title (VARCHAR)
- description (TEXT)
- order_num (INT)
```

#### 4. **lessons** - Уроки
```sql
- id (INT, PRIMARY KEY)
- module_id (INT, FOREIGN KEY → modules.id)
- title (VARCHAR)
- content (TEXT)
- video_url (VARCHAR) - YouTube URL
- order_num (INT)
```

#### 5. **user_courses** - Назначение курсов студентам
```sql
- id (INT, PRIMARY KEY)
- user_id (INT, FOREIGN KEY → users.id)
- course_id (INT, FOREIGN KEY → courses.id)
- enrolled_at (TIMESTAMP)
- completed_at (TIMESTAMP)
- progress (INT) - процент прохождения
```

#### 6. **groups** - Группы студентов
```sql
- id (INT, PRIMARY KEY)
- name (VARCHAR)
- course_id (INT, FOREIGN KEY → courses.id)
- teacher_id (INT, FOREIGN KEY → users.id)
```

#### 7. **schedules** - Расписание занятий
```sql
- id (INT, PRIMARY KEY)
- group_id (INT, FOREIGN KEY → groups.id)
- title (VARCHAR)
- start_time (DATETIME)
- end_time (DATETIME)
- location (VARCHAR)
```

#### 8. **tests** - Тесты
```sql
- id (INT, PRIMARY KEY)
- lesson_id (INT, FOREIGN KEY → lessons.id)
- title (VARCHAR)
- description (TEXT)
- passing_score (INT) - минимальный балл
- time_limit (INT) - лимит времени в минутах
- max_attempts (INT)
```

#### 9. **homeworks** - Домашние задания
```sql
- id (INT, PRIMARY KEY)
- lesson_id (INT, FOREIGN KEY → lessons.id)
- title (VARCHAR)
- description (TEXT)
- due_date (DATETIME)
```

#### 10. **messages** - Сообщения
```sql
- id (INT, PRIMARY KEY)
- sender_id (INT, FOREIGN KEY → users.id)
- receiver_id (INT, FOREIGN KEY → users.id)
- message (TEXT)
- is_read (TINYINT)
- created_at (TIMESTAMP)
```

### Схема связей:

```
users (1) ──< (N) user_courses (N) >── (1) courses
users (1) ──< (N) groups (N) >── (1) courses
courses (1) ──< (N) modules (1) ──< (N) lessons
lessons (1) ──< (N) tests
lessons (1) ──< (N) homeworks
groups (1) ──< (N) schedules
users (1) ──< (N) messages (N) >── (1) users
```

---

## 🔧 Основные компоненты

### 1. Session Management (`core/Session.php`)

Управление сессиями пользователей:

```php
// Проверка авторизации
Session::isLoggedIn();

// Получение данных пользователя
$userId = Session::getUserId();
$userRole = Session::getUserRole();

// Требование авторизации
Session::requireLogin();

// Требование определенной роли
Session::requireRole('admin');
```

### 2. Language System (`core/Language.php`)

Мультиязычность:

```php
// Инициализация
Language::init();

// Получение перевода
echo __('common.save'); // "Сохранить" / "Save" / "Сақтау"

// Получение текущего языка
$lang = Language::getCurrentLanguage(); // 'ru', 'kk', 'en'

// Смена языка
Language::setLanguage('en');
```

### 3. Model Base Class (`core/Model.php`)

Базовый класс для работы с БД:

```php
class User extends Model {
    // Автоматическое подключение к БД через PDO
    // Методы: find(), findAll(), create(), update(), delete()
}
```

### 4. Controller Base Class (`core/Controller.php`)

Базовый класс контроллера:

```php
class AdminController extends Controller {
    // Загрузка модели
    $userModel = $this->model('User');
    
    // Рендер представления
    $this->view('admin/users', ['users' => $users]);
    
    // Редирект
    $this->redirect('admin/dashboard');
    
    // JSON ответ
    $this->json(['status' => 'success']);
}
```

---

## 👥 Роли пользователей

### 1. **Администратор (admin)**

**Возможности:**
- ✅ Управление всеми пользователями (создание, редактирование, удаление)
- ✅ Управление курсами (создание, редактирование, публикация)
- ✅ Создание модулей и уроков
- ✅ Управление группами студентов
- ✅ Создание расписания занятий
- ✅ Управление тестами и вопросами
- ✅ Создание достижений
- ✅ Назначение курсов студентам
- ✅ Просмотр статистики
- ✅ Управление подписками и платежами

**Основные маршруты:**
- `/index.php?url=admin/dashboard` - Главная панель
- `/index.php?url=admin/users` - Управление пользователями
- `/index.php?url=admin/courses` - Управление курсами
- `/index.php?url=admin/groups` - Управление группами
- `/index.php?url=admin/schedule` - Расписание

### 2. **Преподаватель (teacher)**

**Возможности:**
- ✅ Просмотр своих курсов
- ✅ Управление уроками (просмотр структуры)
- ✅ Создание домашних заданий
- ✅ Оценивание работ студентов
- ✅ Просмотр студентов своих курсов
- ✅ Просмотр расписания своих занятий
- ✅ Отметка посещаемости
- ✅ Общение со студентами (сообщения)
- ✅ Просмотр отчетов (расчет зарплаты)

**Основные маршруты:**
- `/index.php?url=teacher/dashboard` - Главная панель
- `/index.php?url=teacher/manageCourse/{id}` - Управление курсом
- `/index.php?url=teacher/reports` - Отчеты
- `/index.php?url=teacher/schedule` - Расписание
- `/index.php?url=teacher/messages` - Сообщения

### 3. **Студент (student)**

**Возможности:**
- ✅ Просмотр назначенных курсов
- ✅ Прохождение уроков (видео, текст)
- ✅ Прохождение тестов
- ✅ Выполнение домашних заданий
- ✅ Просмотр расписания занятий
- ✅ Просмотр достижений
- ✅ Получение сертификатов
- ✅ Общение с преподавателями
- ✅ Просмотр статистики прогресса

**Основные маршруты:**
- `/index.php?url=student/dashboard` - Главная панель
- `/index.php?url=student/course/{id}` - Просмотр курса
- `/index.php?url=student/lesson/{id}` - Просмотр урока
- `/index.php?url=student/test/{id}` - Прохождение теста
- `/index.php?url=student/schedule` - Расписание
- `/index.php?url=student/achievements` - Достижения

### 4. **Менеджер (manager)**

**Возможности:**
- ✅ Просмотр групп
- ✅ Управление группами (создание, редактирование)
- ✅ Просмотр студентов
- ✅ Просмотр статистики

**Основные маршруты:**
- `/index.php?url=manager/dashboard` - Главная панель
- `/index.php?url=manager/groups` - Управление группами

---

## 🌐 API и маршруты

### Авторизация

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/index.php?url=auth/login` | Страница входа |
| POST | `/index.php?url=auth/login` | Авторизация |
| GET | `/index.php?url=auth/register` | Страница регистрации |
| POST | `/index.php?url=auth/register` | Регистрация |
| GET | `/index.php?url=auth/logout` | Выход |

### Администратор

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/index.php?url=admin/dashboard` | Главная панель |
| GET | `/index.php?url=admin/users` | Список пользователей |
| GET | `/index.php?url=admin/createUser` | Форма создания пользователя |
| POST | `/index.php?url=admin/createUser` | Создание пользователя |
| GET | `/index.php?url=admin/editUser/{id}` | Форма редактирования |
| POST | `/index.php?url=admin/editUser/{id}` | Обновление пользователя |
| GET | `/index.php?url=admin/courses` | Список курсов |
| GET | `/index.php?url=admin/createCourse` | Форма создания курса |
| POST | `/index.php?url=admin/createCourse` | Создание курса |
| GET | `/index.php?url=admin/manageCourse/{id}` | Управление курсом |
| GET | `/index.php?url=admin/groups` | Список групп |
| GET | `/index.php?url=admin/schedule` | Расписание |
| GET | `/index.php?url=admin/getScheduleEvents` | API: события календаря (JSON) |
| POST | `/index.php?url=admin/addScheduleEvent` | API: добавление события (JSON) |

### Преподаватель

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/index.php?url=teacher/dashboard` | Главная панель |
| GET | `/index.php?url=teacher/manageCourse/{id}` | Управление курсом |
| GET | `/index.php?url=teacher/courseStudents/{id}` | Студенты курса |
| GET | `/index.php?url=teacher/reports` | Отчеты |
| POST | `/index.php?url=teacher/reports` | Генерация отчета |
| GET | `/index.php?url=teacher/schedule` | Расписание |
| GET | `/index.php?url=teacher/messages` | Сообщения |
| GET | `/index.php?url=teacher/conversation/{id}` | Переписка |
| POST | `/index.php?url=teacher/sendMessage` | Отправка сообщения |
| GET | `/index.php?url=teacher/createHomework/{lesson_id}` | Форма создания ДЗ |
| POST | `/index.php?url=teacher/saveHomework` | Сохранение ДЗ |
| GET | `/index.php?url=teacher/viewSubmissions/{id}` | Работы студентов |
| GET | `/index.php?url=teacher/gradeSubmission/{id}` | Форма оценивания |
| POST | `/index.php?url=teacher/saveGrade` | Сохранение оценки |

### Студент

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/index.php?url=student/dashboard` | Главная панель |
| GET | `/index.php?url=student/course/{id}` | Просмотр курса |
| GET | `/index.php?url=student/lesson/{id}` | Просмотр урока |
| POST | `/index.php?url=student/completeLesson/{id}` | Отметка урока как пройденного |
| GET | `/index.php?url=student/test/{id}` | Прохождение теста |
| POST | `/index.php?url=student/submitTest/{id}` | Отправка теста |
| GET | `/index.php?url=student/testResult/{id}` | Результат теста |
| GET | `/index.php?url=student/schedule` | Расписание |
| GET | `/index.php?url=student/getMyScheduleEvents` | API: события календаря (JSON) |
| GET | `/index.php?url=student/messages` | Сообщения |
| GET | `/index.php?url=student/conversation/{id}` | Переписка |
| POST | `/index.php?url=student/sendMessage` | Отправка сообщения |
| GET | `/index.php?url=student/achievements` | Достижения |
| GET | `/index.php?url=student/certificates` | Сертификаты |
| GET | `/index.php?url=student/profile` | Профиль |
| POST | `/index.php?url=student/updateProfile` | Обновление профиля |

---

## 🔒 Безопасность

### 1. **Хеширование паролей**

Используется `password_hash()` с алгоритмом `bcrypt`:

```php
// Создание хеша
$hash = password_hash($password, PASSWORD_DEFAULT);

// Проверка пароля
if (password_verify($password, $hash)) {
    // Пароль верный
}
```

### 2. **Prepared Statements**

Все SQL-запросы используют подготовленные запросы для защиты от SQL-инъекций:

```php
$stmt = $this->db->prepare("SELECT * FROM users WHERE email = ?");
$stmt->execute([$email]);
```

### 3. **Валидация входных данных**

Все данные от пользователя валидируются:

```php
// Проверка email
filter_var($email, FILTER_VALIDATE_EMAIL);

// Санитизация строк
htmlspecialchars($input, ENT_QUOTES, 'UTF-8');

// Проверка типов
is_numeric($id);
```

### 4. **Защита от CSRF**

Используется проверка сессий для защиты от CSRF-атак.

### 5. **Контроль доступа**

Проверка ролей перед доступом к страницам:

```php
Session::requireRole('admin');
```

### 6. **Защита файлов**

Загруженные файлы сохраняются вне веб-корня или с ограниченным доступом.

---

## 🌍 Мультиязычность

### Структура переводов

Файлы переводов находятся в `lang/`:
- `kk.php` - Казахский
- `ru.php` - Русский
- `en.php` - Английский

### Формат файла переводов:

```php
// lang/ru.php
return [
    'common' => [
        'save' => 'Сохранить',
        'cancel' => 'Отмена',
        'edit' => 'Редактировать',
        'delete' => 'Удалить',
    ],
    'auth' => [
        'login' => 'Вход',
        'email' => 'Email',
        'password' => 'Пароль',
    ],
];
```

### Использование в коде:

```php
// В PHP
echo __('common.save');

// В HTML
<button><?php echo __('common.save'); ?></button>
```

### Переключение языка:

```php
// Через GET параметр
/index.php?url=auth/login&lang=en

// Через сессию
Language::setLanguage('en');
```

---

## 📝 Примеры использования

### Пример 1: Создание контроллера

```php
// controllers/ExampleController.php
<?php

class ExampleController extends Controller {
    
    public function index() {
        Session::requireRole('admin');
        
        $userModel = $this->model('User');
        $users = $userModel->findAll();
        
        $this->view('admin/users', [
            'users' => $users,
            'title' => 'Пользователи'
        ]);
    }
    
    public function create() {
        Session::requireRole('admin');
        
        if ($_SERVER['REQUEST_METHOD'] === 'POST') {
            $userModel = $this->model('User');
            $userModel->createUser($_POST);
            $this->redirect('admin/users');
        }
        
        $this->view('admin/user_form', [
            'title' => 'Создать пользователя'
        ]);
    }
}
```

### Пример 2: Создание модели

```php
// models/Example.php
<?php

class Example extends Model {
    
    public function findAll() {
        $stmt = $this->db->prepare("SELECT * FROM examples ORDER BY id DESC");
        $stmt->execute();
        return $stmt->fetchAll(PDO::FETCH_ASSOC);
    }
    
    public function findById($id) {
        $stmt = $this->db->prepare("SELECT * FROM examples WHERE id = ?");
        $stmt->execute([$id]);
        return $stmt->fetch(PDO::FETCH_ASSOC);
    }
    
    public function create($data) {
        $stmt = $this->db->prepare("
            INSERT INTO examples (title, description) 
            VALUES (?, ?)
        ");
        $stmt->execute([$data['title'], $data['description']]);
        return $this->db->lastInsertId();
    }
}
```

### Пример 3: Создание представления

```php
// views/example/list.php
<?php $title = __('example.list'); include 'views/layouts/header.php'; ?>

<div class="row m-b-sm">
    <div class="col-sm-12">
        <h2 class="font-bold m-b-xs"><?php echo __('example.list'); ?></h2>
    </div>
</div>

<div class="row">
    <div class="col-sm-12">
        <div class="ibox-content border-bottom">
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th><?php echo __('example.title'); ?></th>
                            <th><?php echo __('admin.actions'); ?></th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($items as $item): ?>
                        <tr>
                            <td><?php echo $item['id']; ?></td>
                            <td><?php echo htmlspecialchars($item['title']); ?></td>
                            <td>
                                <a href="/index.php?url=example/edit/<?php echo $item['id']; ?>" 
                                   class="btn btn-xs btn-default">
                                    <?php echo __('common.edit'); ?>
                                </a>
                            </td>
                        </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<?php include 'views/layouts/footer.php'; ?>
```

### Пример 4: AJAX запрос

```javascript
// JavaScript
fetch('/index.php?url=admin/getScheduleEvents')
    .then(response => response.json())
    .then(data => {
        // Обработка данных
        console.log(data);
    })
    .catch(error => {
        console.error('Error:', error);
    });
```

```php
// В контроллере
public function getScheduleEvents() {
    $scheduleModel = $this->model('Schedule');
    $events = $scheduleModel->getEventsForCalendar();
    $this->json($events);
}
```

---

## 🚀 Установка и запуск

### Требования:
- PHP 7.4+
- MySQL 5.7+
- Веб-сервер (Apache/Nginx) или встроенный PHP сервер

### Шаги установки:

1. **Клонирование/загрузка проекта**
   ```bash
   cd /path/to/project
   ```

2. **Создание базы данных**
   ```sql
   CREATE DATABASE lms_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

3. **Импорт схемы**
   ```bash
   mysql -u root -p lms_db < database/complete_schema.sql
   ```

4. **Настройка конфигурации**
   ```php
   // config/config.php
   define('DB_HOST', 'localhost');
   define('DB_NAME', 'lms_db');
   define('DB_USER', 'root');
   define('DB_PASS', 'your_password');
   ```

5. **Права доступа**
   ```bash
   chmod -R 755 public/uploads/
   ```

6. **Запуск**
   ```bash
   # Встроенный сервер (для разработки)
   php -S localhost:8000
   
   # Или через Apache/Nginx
   # Настроить DocumentRoot на корень проекта
   ```

7. **Вход в систему**
   - URL: `http://localhost:8000/index.php?url=auth/login`
   - Email: `admin@lms.com`
   - Password: `password`

---

## 📊 Диаграммы

### Поток авторизации:

```
Пользователь → /auth/login (GET)
    ↓
Форма входа
    ↓
POST /auth/login
    ↓
AuthController::login()
    ↓
Проверка email/password
    ↓
Создание сессии
    ↓
Редирект на dashboard (по роли)
```

### Поток создания курса:

```
Admin → /admin/createCourse (GET)
    ↓
Форма создания
    ↓
POST /admin/createCourse
    ↓
AdminController::createCourse()
    ↓
Валидация данных
    ↓
Загрузка изображения
    ↓
Сохранение в БД (Course Model)
    ↓
Редирект на /admin/courses
```

---

## 🔍 Отладка

### Включение отображения ошибок:

```php
// В начале index.php (только для разработки!)
error_reporting(E_ALL);
ini_set('display_errors', 1);
```

### Логирование:

```php
// Запись в лог
error_log("Error message: " . $error, 3, "logs/error.log");
```

### Проверка подключения к БД:

```php
// В config/database.php
try {
    $pdo = new PDO(...);
    echo "Connected successfully";
} catch(PDOException $e) {
    echo "Connection failed: " . $e->getMessage();
}
```

---

## 📞 Контакты и поддержка

**Проект:** ATAMEKEN STUDY CRM  
**Версия:** 1.0  
**Дата:** 2024

---

## 📄 Лицензия

Проект разработан для внутреннего использования.

---

**Документация обновлена:** 2024

