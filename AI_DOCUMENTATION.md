# 📚 ПОЛНАЯ ДОКУМЕНТАЦИЯ ПРОЕКТА ATAMEKEN STUDY CRM

## 🎯 ОПИСАНИЕ ПРОЕКТА

Проект представляет собой систему управления обучением (CRM для образовательного учреждения) под названием **ATAMEKEN STUDY CRM**. Система предназначена для управления учебным процессом, студентами, учителями, финансами, расписанием и коммуникацией между участниками образовательного процесса.

**Целевая версия:** 2.0 (ATAMEKEN STUDY CRM)  
**Текущая версия:** 1.0 (базовая LMS)  
**Процент готовности:** ~35%

---

## 🛠 ТЕХНОЛОГИЧЕСКИЙ СТЕК (СТАК)

### Backend (Серверная часть)

**Язык программирования:**
- PHP версия 7.4 или выше (рекомендуется PHP 8.0+)
- Чистый PHP без использования фреймворков (Laravel, Symfony, CodeIgniter и т.д. запрещены)
- Никаких внешних библиотек и зависимостей, кроме стандартных расширений PHP

**Архитектура:**
- Паттерн MVC (Model-View-Controller)
- Кастомная реализация без использования готовых фреймворков
- Собственный роутинг через класс Router

**База данных:**
- MySQL версия 5.7 или выше (рекомендуется MySQL 8.0+)
- Кодировка: utf8mb4 с collation utf8mb4_unicode_ci
- Работа через PDO (PHP Data Objects) с использованием prepared statements для защиты от SQL-инъекций

**Сервер:**
- Веб-сервер: Apache или Nginx
- Для разработки можно использовать встроенный PHP сервер: `php -S localhost:8000`

### Frontend (Клиентская часть)

**Языки разметки и стилей:**
- HTML5 (семантическая разметка)
- CSS3 (включая современные возможности: flexbox, grid, transitions, animations)
- Без использования CSS-препроцессоров (SASS, LESS запрещены) или с их компиляцией на этапе разработки

**Язык программирования:**
- JavaScript (Vanilla JS - чистый JavaScript)
- Запрещены фреймворки и библиотеки: React, Vue.js, Angular, jQuery (кроме случаев, когда требуется для готовых компонентов)
- Можно использовать нативный JavaScript для AJAX, DOM-манипуляций, событий

**Шрифты:**
- Google Fonts (Inter) подключен через CDN

### Работа с файлами

**Загрузка файлов:**
- Изображения: аватары пользователей, изображения курсов, иконки достижений
- Документы: материалы для уроков, домашние задания
- Видео: НЕ загружаются напрямую, используется встраивание YouTube ссылок через iframe

**Структура папок для загрузок:**
- `public/uploads/avatars/` - аватары пользователей
- `public/uploads/course_images/` - изображения курсов
- `public/uploads/achievement_icons/` - иконки достижений
- `public/uploads/documents/` - документы и файлы

### Международная поддержка (i18n)

**Мультиязычность:**
- Система поддерживает 3 языка:
  - Казахский (kk) - основной язык по умолчанию
  - Русский (ru)
  - Английский (en)
- Переводы хранятся в файлах: `lang/kk.php`, `lang/ru.php`, `lang/en.php`
- Класс `Language` для управления языками
- Функция `__()` для получения переводов в представлениях

---

## 🚫 ЗАПРЕЩЕННЫЕ PHP ФУНКЦИИ (ПОЛНЫЙ СПИСОК)

**КРИТИЧЕСКИ ВАЖНО:** Проект должен быть реализован **ПОЛНОСТЬЮ БЕЗ ИСПОЛЬЗОВАНИЯ** следующих PHP функций и возможностей. Эти функции **ЗАПРЕЩЕНЫ К ИСПОЛЬЗОВАНИЮ** в любой части кода проекта:

**Запрещенные функции PHP:**
```
dl	exec	openlog	passthru	pcntl_exec	pcntl_fork	popen	posix_getpwuid	posix_kill	posix_mkfifo	posix_setpgid	posix_setsid	posix_setuid	proc_close	proc_get_status	proc_nice	proc_open	proc_terminate	shell_exec	socket_create	socket_create_listen	socket_create_pair	syslog	system	socket_listen	stream_socket_server
```

**ПРИМЕЧАНИЕ:** Если какая-либо функциональность требует использования этих функций, необходимо найти альтернативный подход или полностью отказаться от этой функции. Проект должен работать исключительно в рамках разрешенных возможностей PHP.

---

## 📁 СТРУКТУРА ПРОЕКТА

```
LMSSS_AKUUU/
│
├── config/                    # Конфигурационные файлы
│   ├── config.php            # Основные настройки (BASE_URL, DB и т.д.)
│   └── database.php          # Подключение к базе данных (PDO)
│
├── core/                      # Ядро системы (MVC компоненты)
│   ├── Router.php            # Кастомный роутер для маршрутизации
│   ├── Controller.php       # Базовый класс контроллера
│   ├── Model.php             # Базовый класс модели
│   ├── Session.php           # Управление сессиями и доступом
│   └── Language.php          # Система переводов и языков
│
├── controllers/               # Контроллеры (бизнес-логика)
│   ├── AuthController.php    # Аутентификация (регистрация, вход, выход)
│   ├── AdminController.php   # Административная панель
│   ├── StudentController.php # Функционал для студентов
│   ├── TeacherController.php # Функционал для учителей
│   ├── TestController.php    # Управление тестами
│   └── WeeklyTestController.php # Недельные обязательные тесты
│
├── models/                    # Модели (работа с БД)
│   ├── User.php              # Пользователи
│   ├── Course.php            # Курсы
│   ├── Module.php            # Модули курсов
│   ├── Lesson.php            # Уроки
│   ├── Test.php              # Тесты
│   ├── WeeklyTest.php        # Недельные тесты
│   ├── UserCourse.php        # Записи студентов на курсы
│   ├── UserProgress.php      # Прогресс студентов
│   ├── Achievement.php       # Достижения/бейджи
│   ├── Certificate.php      # Сертификаты
│   ├── Message.php           # Сообщения
│   └── Statistics.php        # Статистика
│
├── views/                     # Представления (HTML шаблоны)
│   ├── layouts/
│   │   ├── header.php        # Шапка сайта (навигация)
│   │   └── footer.php        # Подвал сайта
│   │
│   ├── landing/
│   │   └── index.php         # Landing page (главная страница)
│   │
│   ├── auth/
│   │   ├── login.php         # Страница входа
│   │   └── register.php      # Страница регистрации
│   │
│   ├── admin/                 # Админ панель
│   │   ├── dashboard.php     # Дашборд админа
│   │   ├── users.php         # Список пользователей
│   │   ├── user_form.php     # Форма создания/редактирования пользователя
│   │   ├── courses.php       # Список курсов
│   │   ├── course_form.php   # Форма создания/редактирования курса
│   │   ├── manage_course.php # Управление курсом (модули, уроки, тесты)
│   │   ├── achievements.php  # Достижения
│   │   ├── achievement_form.php # Форма создания достижения
│   │   ├── enrollments.php   # Записи студентов на курсы
│   │   ├── statistics.php    # Статистика студентов
│   │   ├── test_builder.php  # Создание тестов
│   │   └── weekly_test_form.php # Форма недельных тестов
│   │
│   ├── student/               # Интерфейс студента
│   │   ├── dashboard.php     # Дашборд студента
│   │   ├── course.php         # Просмотр курса
│   │   ├── lesson.php        # Просмотр урока
│   │   ├── test.php           # Прохождение теста
│   │   ├── test_result.php   # Результат теста
│   │   ├── weekly_test.php   # Недельный тест
│   │   ├── weekly_test_result.php # Результат недельного теста
│   │   ├── achievements.php  # Достижения студента
│   │   ├── certificates.php  # Сертификаты студента
│   │   ├── certificate_view.php # Просмотр сертификата
│   │   ├── messages.php      # Список сообщений
│   │   ├── conversation.php  # Переписка с учителем
│   │   ├── my_teachers.php   # Список учителей студента
│   │   ├── profile.php       # Профиль студента
│   │   └── statistics.php    # Статистика студента
│   │
│   └── teacher/               # Интерфейс учителя
│       ├── dashboard.php     # Дашборд учителя
│       ├── messages.php       # Сообщения
│       ├── conversation.php   # Переписка
│       └── course_students.php # Студенты курса
│
├── lang/                       # Файлы переводов
│   ├── kk.php                 # Казахский язык (основной)
│   ├── ru.php                 # Русский язык
│   └── en.php                 # Английский язык
│
├── public/                     # Публичные файлы (доступны напрямую)
│   ├── css/
│   │   ├── style.css         # Основные стили приложения
│   │   └── landing.css       # Стили для landing page
│   ├── js/
│   │   └── main.js           # JavaScript функционал
│   ├── images/               # Изображения (если нужны)
│   └── uploads/              # Загруженные файлы
│       ├── avatars/          # Аватары пользователей
│       ├── course_images/   # Изображения курсов
│       ├── achievement_icons/ # Иконки достижений
│       └── documents/       # Документы
│
├── database/                   # SQL скрипты
│   ├── schema.sql            # Основная схема базы данных
│   └── weekly_tests_update.sql # Обновление для недельных тестов
│
├── index.php                   # Точка входа в приложение
│
└── [файлы документации]
    ├── README.md
    ├── INSTALL.md
    ├── START.md
    ├── LANGUAGES.md
    ├── DESIGN.md
    ├── FINAL.md
    ├── PROJECT_STATUS.md
    └── AI_DOCUMENTATION.md (этот файл)
```

---

## ✅ ТЕКУЩИЙ СТАТУС ПРОЕКТА (ЧТО РЕАЛИЗОВАНО)

### 1. АУТЕНТИФИКАЦИЯ И РОЛИ ПОЛЬЗОВАТЕЛЕЙ

**Реализовано:**
- ✅ Регистрация новых пользователей (`/index.php?url=auth/register`)
- ✅ Вход в систему (`/index.php?url=auth/login`)
- ✅ Выход из системы (`/index.php?url=auth/logout`)
- ✅ Хеширование паролей через `password_hash()` и проверка через `password_verify()`
- ✅ Управление сессиями через класс `Session`
- ✅ Роль "admin" (администратор) - полный доступ ко всем функциям
- ✅ Роль "teacher" (учитель) - управление курсами, просмотр студентов, сообщения
- ✅ Роль "student" (студент) - прохождение курсов, тесты, сертификаты

**Файлы:**
- `controllers/AuthController.php`
- `core/Session.php`
- `models/User.php`
- `views/auth/login.php`
- `views/auth/register.php`
- База данных: таблица `users` с полями: id, email, password, first_name, last_name, role, avatar, is_active, created_at, updated_at

**Отсутствует:**
- ❌ Роль "manager" (менеджер) - не реализована
- ❌ Роль "parent" (родитель) - не реализована

---

### 2. УПРАВЛЕНИЕ КУРСАМИ

**Реализовано:**
- ✅ Создание курсов администратором (`/admin/courses`)
- ✅ Редактирование курсов
- ✅ Назначение учителя на курс
- ✅ Публикация/скрытие курсов
- ✅ Структура курса: Курс → Модули → Уроки
- ✅ Управление модулями (`/admin/manageCourse/{id}`)
- ✅ Управление уроками внутри модулей
- ✅ Загрузка изображений для курсов
- ✅ YouTube видео встраивание (через iframe)

**Файлы:**
- `controllers/AdminController.php` - методы: `courses()`, `createCourse()`, `editCourse()`, `manageCourse()`
- `models/Course.php`
- `models/Module.php`
- `models/Lesson.php`
- База данных: таблицы `courses`, `modules`, `lessons`

**Отсутствует:**
- ❌ Группы студентов
- ❌ Расписание занятий
- ❌ Типы аудиторий (класс, онлайн, лаборатория)

---

### 3. СИСТЕМА ТЕСТИРОВАНИЯ

**Реализовано:**
- ✅ Создание тестов (`/admin/manageCourse/{id}`)
- ✅ Типы вопросов:
  - Одиночный выбор (`single_choice`)
  - Множественный выбор (`multiple_choice`)
  - Текстовый ответ (`text`)
- ✅ Проходной балл (passing_score)
- ✅ Ограничение по времени (time_limit в минутах)
- ✅ Ограничение попыток (max_attempts)
- ✅ Недельные обязательные тесты (`/test/createWeeklyTest/{course_id}`)
- ✅ Прохождение тестов студентами (`/student/test/{id}`, `/student/weeklyTest/take/{id}`)
- ✅ Результаты тестов с детальной информацией
- ✅ Статистика по тестам в профиле студента

**Файлы:**
- `controllers/TestController.php`
- `controllers/WeeklyTestController.php`
- `models/Test.php`
- `models/WeeklyTest.php`
- База данных: таблицы `tests`, `test_questions`, `test_answers`, `user_test_results`, `weekly_tests`, `weekly_test_questions`, `weekly_test_answers`, `user_weekly_test_results`, `user_weekly_test_attempts`

**Отсутствует:**
- ❌ Публичный рейтинг всех студентов (видимый для всех)
- ❌ Система оценок (отдельно от тестов)

---

### 4. ПРОГРЕСС И СТАТИСТИКА

**Реализовано:**
- ✅ Отслеживание прогресса по урокам (завершен/не завершен)
- ✅ Отслеживание завершения курсов
- ✅ Личная статистика студента (`/student/statistics`):
  - Количество записанных курсов
  - Количество завершенных курсов
  - Средний балл по тестам
  - Прогресс по каждому курсу
  - Результаты последних тестов
  - Хронология активности (пройденные уроки)
- ✅ Общая статистика для админа (`/admin/statistics`):
  - Список всех студентов
  - Средний балл каждого
  - Последняя активность

**Файлы:**
- `models/UserProgress.php`
- `models/UserCourse.php`
- `models/Statistics.php`
- `controllers/StudentController.php` - метод `statistics()`
- `controllers/AdminController.php` - метод `statistics()`
- База данных: таблицы `user_progress`, `user_courses`, `user_test_results`

---

### 5. ДОСТИЖЕНИЯ И СЕРТИФИКАТЫ

**Реализовано:**
- ✅ Система достижений/бейджей (`/admin/achievements`)
- ✅ Типы достижений:
  - Завершение курса (`course_complete`)
  - Балл в тесте (`test_score`)
  - Количество курсов (`courses_count`)
  - Идеальный результат (`perfect_test`)
- ✅ Автоматическая выдача достижений при выполнении условий
- ✅ Автоматическая генерация сертификатов при завершении курса
- ✅ Уникальный номер сертификата
- ✅ Просмотр сертификатов (`/student/certificates`)

**Файлы:**
- `models/Achievement.php`
- `models/Certificate.php`
- База данных: таблицы `achievements`, `user_achievements`, `certificates`, `user_certificates`

---

### 6. СИСТЕМА СООБЩЕНИЙ

**Реализовано:**
- ✅ Личные сообщения между студентом и учителем
- ✅ Список переписок (`/student/messages`, `/teacher/messages`)
- ✅ Открытие конкретной переписки (`/student/conversation/{teacher_id}`, `/teacher/conversation/{student_id}`)
- ✅ Отправка сообщений (AJAX или обычная форма)
- ✅ Отметка о прочтении сообщений
- ✅ Список учителей студента (`/student/myTeachers`)

**Файлы:**
- `models/Message.php`
- `controllers/StudentController.php` - методы: `messages()`, `conversation()`, `sendMessage()`, `myTeachers()`
- `controllers/TeacherController.php` - методы: `messages()`, `conversation()`, `sendMessage()`, `courseStudents()`
- База данных: таблица `messages` с полями: id, sender_id, receiver_id, course_id, subject, message, is_read, created_at

**Отсутствует:**
- ❌ Групповые чаты
- ❌ Чаты по группам (автоматическое добавление студентов группы в чат)
- ❌ Сообщения студент-студент
- ❌ Сообщения учитель-учитель

---

### 7. ЛАНДИНГ ПЕЙДЖ

**Реализовано:**
- ✅ Главная страница (landing page) при неавторизованном доступе
- ✅ Секции: Hero, Features, Stats, How It Works, CTA, Footer
- ✅ Мультиязычность на главной странице
- ✅ Переключатель языка
- ✅ Кнопки регистрации и входа

**Файлы:**
- `views/landing/index.php`
- `public/css/landing.css`

---

### 8. МУЛЬТИЯЗЫЧНОСТЬ

**Реализовано:**
- ✅ Поддержка 3 языков: Казахский (kk), Русский (ru), Английский (en)
- ✅ Система переводов через класс `Language`
- ✅ Функция `__()` для получения переводов
- ✅ Переключатель языка в навигации и на страницах аутентификации
- ✅ Сохранение выбранного языка в сессии

**Файлы:**
- `core/Language.php`
- `lang/kk.php`, `lang/ru.php`, `lang/en.php`
- Переводы для всех основных разделов: навигация, аутентификация, курсы, тесты, сообщения, админ панель, статистика

---

### 9. АДАПТИВНЫЙ ДИЗАЙН

**Реализовано:**
- ✅ Полностью адаптивный дизайн для:
  - Десктоп (1920px и выше)
  - Планшеты (1024px - 768px)
  - Мобильные устройства (768px и меньше)
- ✅ Мобильное меню с кнопкой-гамбургером
- ✅ Современный дизайн с градиентами, тенями, анимациями
- ✅ Glassmorphism эффекты
- ✅ Плавные переходы и hover эффекты

**Файлы:**
- `public/css/style.css` (1800+ строк CSS)
- `public/css/landing.css`

---

## ❌ ЧТО НЕ РЕАЛИЗОВАНО (НУЖНО ДОРАБОТАТЬ)

### 1. ГРУППЫ СТУДЕНТОВ

**Что нужно реализовать:**
- Создать таблицы: `groups`, `group_members`, `group_schedule`
- Создать модель: `Group.php`
- Создать контроллер: `GroupController.php`
- Создать представления:
  - `views/admin/groups.php` - список групп
  - `views/admin/group_form.php` - форма создания/редактирования группы
  - `views/admin/group_details.php` - детали группы
- Функционал:
  - Создание группы (название, курс, учитель, день-время, вместимость)
  - Автоматическая или ручная регистрация студентов в группу
  - Список открытых групп с фильтрацией
  - Управление участниками группы

---

### 2. РАСПИСАНИЕ ЗАНЯТИЙ

**Что нужно реализовать:**
- Создать таблицы: `classes`, `class_schedule`, `class_attendance`
- Создать модель: `Class.php`, `Schedule.php`, `Attendance.php`
- Создать контроллер: `ScheduleController.php`
- Создать представления:
  - `views/admin/schedule.php` - календарь расписания
  - `views/student/schedule.php` - расписание студента
  - `views/teacher/schedule.php` - расписание учителя
- Функционал:
  - Просмотр в дневном, недельном, месячном виде
  - Фильтрация по аудитории (класс, онлайн, лаборатория)
  - Добавление/изменение/удаление занятий
  - Отметка присутствия студентов
  - Отчеты по посещаемости

---

### 3. ФИНАНСОВЫЙ РАЗДЕЛ

**Что нужно реализовать:**
- Создать таблицы: `payments`, `subscriptions`, `subscription_classes`, `expenses`
- Создать модель: `Payment.php`, `Subscription.php`, `Expense.php`
- Создать контроллер: `FinanceController.php`
- Создать представления:
  - `views/admin/finance.php` - финансовая панель
  - `views/admin/payments.php` - история платежей
  - `views/admin/subscriptions.php` - абонементы
  - `views/student/payments.php` - платежи студента
- Функционал:
  - Отображение платежей каждого студента
  - Отчет по финансам (поступившие платежи, долги, история)
  - Прогноз оплаты
  - Управление абонементами (создание, приостановка, продление)
  - Автоматический расчет стоимости занятия
  - Автоматическое списание оплаты с абонемента
  - Учет расходов (зарплаты учителей, коммунальные, инвентарь)

---

### 4. РОЛЬ МЕНЕДЖЕРА

**Что нужно реализовать:**
- Добавить роль "manager" в ENUM поля таблицы `users`
- Обновить класс `Session` для поддержки роли менеджера
- Создать контроллер: `ManagerController.php`
- Создать представления для менеджера
- Функционал:
  - Регистрация студентов в группы
  - Доступ ко всей информации по студенту
  - Управление записями на курсы

---

### 5. ГРУППОВЫЕ ЧАТЫ

**Что нужно реализовать:**
- Создать таблицы: `group_chat`, `group_members`
- Расширить модель `Message.php` для поддержки групповых сообщений
- Обновить контроллеры для групповых чатов
- Функционал:
  - Автоматическое добавление студентов группы в групповой чат
  - Групповой чат для каждой открытой группы
  - Сохранение всех сообщений навсегда
  - История сообщений для аудита
  - Поиск сообщений

---

### 6. ДОМАШНИЕ ЗАДАНИЯ

**Что нужно реализовать:**
- Создать таблицы: `homeworks`, `homework_submissions`
- Создать модель: `Homework.php`
- Создать контроллер: `HomeworkController.php`
- Создать представления:
  - `views/teacher/homeworks.php` - список домашних заданий
  - `views/teacher/homework_form.php` - создание домашнего задания
  - `views/student/homeworks.php` - домашние задания студента
  - `views/teacher/homework_submissions.php` - сданные работы
- Функционал:
  - Выдача домашних заданий учителем
  - Сдача домашних заданий студентом
  - Проверка и оценка домашних заданий

---

### 7. ОТЧЕТЫ ДЛЯ УЧИТЕЛЕЙ

**Что нужно реализовать:**
- Добавить методы в `TeacherController.php`
- Создать представление: `views/teacher/reports.php`
- Функционал:
  - Отчет по проведенным занятиям
  - Расчет зарплаты (по проведенным занятиям или фиксированная ставка)
  - Статистика студентов

---

### 8. ДОСТУП РОДИТЕЛЕЙ

**Что нужно реализовать:**
- Добавить роль "parent" в ENUM поля таблицы `users`
- Добавить поле `parent_id` в таблицу `users` для связи студент-родитель
- Создать контроллер: `ParentController.php`
- Создать представления для родителей
- Функционал:
  - Просмотр успеваемости ребенка
  - Просмотр посещаемости
  - Просмотр платежей
  - Просмотр расписания

---

### 9. ИМПОРТ EXCEL

**Что нужно реализовать:**
- Добавить функционал импорта студентов через Excel файл
- Использовать PHP библиотеку для чтения Excel (или написать парсер CSV)
- Создать представление: `views/admin/import_students.php`

---

### 10. УВЕДОМЛЕНИЯ

**Что нужно реализовать:**
- Создать таблицу: `notifications`
- Создать модель: `Notification.php`
- Функционал:
  - SMS/Email напоминание перед занятием
  - SMS/Email напоминание при приближении дня оплаты
  - Внутренние уведомления в системе

---

### 11. ДОПОЛНИТЕЛЬНЫЕ ФУНКЦИИ

**Что нужно реализовать:**
- Контроль посещаемости (отметка присутствия на каждом занятии)
- Индивидуальный учебный план для студентов
- Функционал обратной связи/опросов (оценка занятий, учителей)
- Архив завершенных курсов и занятий
- Функция жалоб (подача жалобы от студентов анонимно или не анонимно)
- Публичный рейтинг студентов (видимый для всех)

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Роутинг

Система использует кастомный роутер **БЕЗ использования `.htaccess`**. Все маршруты идут через `index.php` с параметром `url`.

**Формат URL:**
```
/index.php?url=controller/method/param1/param2
```

**Примеры:**
- `/index.php?url=auth/login` → `AuthController::login()`
- `/index.php?url=admin/users` → `AdminController::users()`
- `/index.php?url=student/course/5` → `StudentController::course(5)`

**Файл:** `core/Router.php`

### Подключение к базе данных

Используется PDO для безопасной работы с MySQL. Подключение через:
```php
// config/database.php
$pdo = new PDO(
    "mysql:host=" . DB_HOST . ";dbname=" . DB_NAME . ";charset=utf8mb4",
    DB_USER,
    DB_PASS,
    [PDO::ATTR_ERRMODE => PDO::ERRMODE_EXCEPTION]
);
```


---

## 📝 ПРИНЦИПЫ РАЗРАБОТКИ

1. **Чистый PHP** - никаких фреймворков
2. **MVC паттерн** - разделение логики, данных и представления
3. **Безопасность** - prepared statements, хеширование паролей, валидация входных данных
4. **Запрещенные функции** - НЕ использовать функции из списка запрещенных
5. **Мультиязычность** - все тексты через систему переводов
6. **Адаптивность** - поддержка всех устройств
7. **Чистый код** - понятные имена, комментарии, структура

---

## 🎯 ПРИОРИТЕТЫ РАЗРАБОТКИ

### 🔴 КРИТИЧЕСКИ ВАЖНО (сначала это):

1. **Группы студентов** - базовая структура для многих функций
2. **Расписание занятий** - критически важно для CRM
3. **Финансовый раздел** - основная часть CRM
4. **Роль Менеджер** - добавление в систему

### 🟡 ВАЖНО (потом это):

5. **Групповые чаты** - расширение системы сообщений
6. **Домашние задания** - популярная функция
7. **Посещаемость** - отметка присутствия
8. **Отчеты для учителей** - расчет зарплаты
9. **Доступ родителей** - просмотр успеваемости
10. **Импорт Excel** - удобство регистрации

### 🟢 ЖЕЛАТЕЛЬНО (можно потом):

11. **Уведомления** - SMS/Email
12. **Публичный рейтинг** - видимый для всех
13. **Индивидуальный план** - для студентов
14. **Обратная связь** - опросы, оценки
15. **Архив** - завершенные курсы
16. **Жалобы** - анонимные/именные

---

## 📞 КОНТАКТНАЯ ИНФОРМАЦИЯ ПРОЕКТА

**База данных:**
- Host: `localhost` (или из конфига)
- Database: `infogok1_AKUUUU` (из `config/config.php`)
- User: `infogok1_AKUUUU`
- Password: `Gabi2005@`

**Учетные данные по умолчанию:**
- Admin: `admin@lms.com` / `password`
- Admin: `gabi@lms.com` / `password`

---

## ✅ ЧЕКЛИСТ ДЛЯ AI-АССИСТЕНТА

Перед началом работы убедись:

- [ ] Прочитал весь этот документ
- [ ] Понял технологический стек
- [ ] Запомнил список запрещенных функций
- [ ] Изучил текущую структуру проекта
- [ ] Понял, что реализовано, а что нет
- [ ] Определил задачу, которую будешь реализовывать
- [ ] Проверил, что не используешь запрещенные функции
- [ ] Следуешь принципам MVC
- [ ] Добавляешь переводы во все 3 языка при добавлении новых текстов
- [ ] Делаешь адаптивный дизайн
- [ ] Используешь prepared statements для всех SQL запросов
- [ ] Проверяешь безопасность (валидация, экранирование)

---

**Дата создания документации:** 2024  
**Версия документации:** 1.0  
**Для AI-ассистента:** Gemini / Claude / ChatGPT

---

## 🔄 ОБНОВЛЕНИЯ ДОКУМЕНТАЦИИ

При добавлении новых функций обновляй этот документ:
1. Добавь реализованную функцию в раздел "ЧТО РЕАЛИЗОВАНО"
2. Удали из раздела "ЧТО НЕ РЕАЛИЗОВАНО"
3. Обнови процент готовности в начале документа

---

## 🔒 БЕЗОПАСНОСТЬ

### Защита от SQL-инъекций

Все запросы к базе данных используют **prepared statements**. Никогда не используй конкатенацию строк для SQL запросов:

**✅ ПРАВИЛЬНО:**
```php
$stmt = $pdo->prepare("SELECT * FROM users WHERE email = :email AND role = :role");
$stmt->execute(['email' => $email, 'role' => $role]);
$user = $stmt->fetch(PDO::FETCH_ASSOC);
```

**❌ НЕПРАВИЛЬНО:**
```php
// ЗАПРЕЩЕНО! Это уязвимо для SQL-инъекций
$query = "SELECT * FROM users WHERE email = '$email'";
$stmt = $pdo->query($query);
```

### Хеширование паролей

Используется `password_hash()` для хеширования и `password_verify()` для проверки. Никогда не храни пароли в открытом виде:

**✅ ПРАВИЛЬНО:**
```php
// При регистрации/создании пользователя
$hashed = password_hash($password, PASSWORD_DEFAULT);

// При проверке пароля
if (password_verify($password, $hashed)) {
    // Пароль верный
}
```

**❌ НЕПРАВИЛЬНО:**
```php
// ЗАПРЕЩЕНО! Нельзя хранить пароли в открытом виде
$password = md5($password); // или sha1, или в открытом виде
```

### Валидация входных данных

Всегда валидируй и экранируй данные от пользователя перед использованием в запросах или выводе:

**✅ ПРАВИЛЬНО:**
```php
// Валидация email
if (!filter_var($email, FILTER_VALIDATE_EMAIL)) {
    // Ошибка: некорректный email
}

// Валидация целого числа
$id = filter_var($_GET['id'], FILTER_VALIDATE_INT);
if ($id === false) {
    // Ошибка: некорректный ID
}

// Экранирование при выводе в HTML
echo htmlspecialchars($user_input, ENT_QUOTES, 'UTF-8');
```

### Управление сессиями

Используй класс `Session` для управления сессиями и контроля доступа:

**Методы класса Session:**
- `Session::login($user)` - вход пользователя (сохраняет данные в сессию)
- `Session::logout()` - выход (уничтожает сессию)
- `Session::isLoggedIn()` - проверка авторизации (возвращает true/false)
- `Session::requireLogin()` - требование авторизации (редирект на логин, если не авторизован)
- `Session::requireRole($role)` - требование определенной роли (редирект, если роль не совпадает)
- `Session::getUserId()` - ID текущего пользователя
- `Session::getUserRole()` - роль текущего пользователя (admin, teacher, student)
- `Session::get($key)` - получение значения из сессии
- `Session::set($key, $value)` - установка значения в сессию

**Пример использования:**
```php
// В контроллере
public function dashboard() {
    Session::requireLogin(); // Проверка авторизации
    Session::requireRole('admin'); // Проверка роли
    
    $userId = Session::getUserId();
    $userRole = Session::getUserRole();
    
    // Далее логика для авторизованного админа
}
```

### Защита от XSS (Cross-Site Scripting)

Всегда экранируй пользовательские данные при выводе в HTML:

**✅ ПРАВИЛЬНО:**
```php
// В представлениях (views)
echo htmlspecialchars($userData, ENT_QUOTES, 'UTF-8');
// Или используй короткую функцию, если она есть
echo e($userData);
```

**❌ НЕПРАВИЛЬНО:**
```php
// ЗАПРЕЩЕНО! Уязвимо для XSS атак
echo $userData; // без экранирования
```

### Защита от CSRF (Cross-Site Request Forgery)

Для форм, изменяющих данные, используй CSRF токены:

**Пример реализации:**
```php
// Генерация токена
$csrfToken = bin2hex(random_bytes(32));
$_SESSION['csrf_token'] = $csrfToken;

// В форме
<input type="hidden" name="csrf_token" value="<?php echo htmlspecialchars($csrfToken); ?>">

// При обработке формы
if (!isset($_POST['csrf_token']) || $_POST['csrf_token'] !== $_SESSION['csrf_token']) {
    die('CSRF token mismatch');
}
```

### Права доступа к файлам

Убедись, что загруженные файлы не выполняются как код:

```php
// Проверка типа файла при загрузке
$allowedTypes = ['image/jpeg', 'image/png', 'application/pdf'];
if (!in_array($_FILES['file']['type'], $allowedTypes)) {
    die('Недопустимый тип файла');
}

// Переименование файла при сохранении
$extension = pathinfo($_FILES['file']['name'], PATHINFO_EXTENSION);
$filename = uniqid() . '.' . $extension;
$uploadPath = UPLOAD_DIR . $filename;
```

### Ограничение доступа к конфигурационным файлам

Конфигурационные файлы должны быть недоступны напрямую через веб-сервер. Они должны находиться вне папки `public/`.

### Логирование ошибок

В продакшене отключай отображение ошибок PHP, но логируй их:

```php
// В config/config.php
if (ENVIRONMENT === 'production') {
    ini_set('display_errors', 0);
    ini_set('log_errors', 1);
    ini_set('error_log', __DIR__ . '/../logs/error.log');
}
```

### Общие принципы безопасности

1. **Принцип наименьших привилегий** - пользователи должны иметь доступ только к тому, что им необходимо
2. **Валидация на сервере** - никогда не полагайся только на клиентскую валидацию
3. **Защита от перебора паролей** - ограничь количество попыток входа
4. **Регулярное обновление** - следи за безопасностью используемых библиотек (если есть)
5. **Шифрование соединений** - используй HTTPS в продакшене
6. **Регулярные бэкапы** - создавай резервные копии базы данных
7. **Логирование действий** - важные действия (изменение пароля, удаление данных) должны логироваться

---

**Дата создания документации:** 2024  
**Версия документации:** 1.0  
**Для AI-ассистента:** Gemini / Claude / ChatGPT

